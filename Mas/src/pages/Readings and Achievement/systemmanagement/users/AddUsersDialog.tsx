import { Button, Box } from "@mui/material";

export default function AddUsersDialog() {
  return (
    <Box sx={{ paddingTop: 1, mb: 1 }}>
      <Button variant="contained">إضافة مستخدم جديد</Button>
    </Box>
  );
}
